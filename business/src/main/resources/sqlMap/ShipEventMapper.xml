<?xml version="1.0" encoding="UTF-8" ?>
<!--  Generate by autoSQLMap Powered by duxing@Taobao -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xingyi.logistic.business.db.dao.ShipEventDAO">
    <resultMap id="AllColumnMap" type="com.xingyi.logistic.business.db.entity.ShipEventDO">
        <result property="id" column="id"/>
        <result property="shipId" column="shipId"/>
        <result property="eventId" column="eventId"/>
        <result property="beginDate" column="beginDate"/>
        <result property="endDate" column="endDate"/>
        <result property="`dayNum" column="`dayNum"/>
        <result property="description" column="description"/>
        <result property="creator" column="creator"/>
        <result property="created" column="created"/>
        <result property="updated" column="updated"/>
    </resultMap>

    <sql id="all_column">
        a.`id`
        ,a.`shipId`
        ,a.`eventId`
        ,a.`beginDate`
        ,a.`endDate`
        ,a.`dayNum`
        ,a.`description`
        ,a.`creator`
        ,a.`created`
        ,a.`updater`
        ,a.`updated`
    </sql>
    <sql id="all_column_user">
        <include refid="all_column"/>
        ,b.`realName` as creatorName
    </sql>
    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="pojo.id">
        INSERT INTO ShipEvent
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="pojo.shipId!=null"> `shipId`,</if>
            <if test="pojo.eventId!=null"> `eventId`,</if>
            <if test="pojo.beginDate!=null"> `beginDate`,</if>
            <if test="pojo.endDate!=null"> `endDate`,</if>
            <if test="pojo.dayNum!=null"> `dayNum`,</if>
            <if test="pojo.description!=null"> `description`,</if>
            <if test="pojo.creator!=null"> `creator`,</if>
            <if test="pojo.updater!=null"> `updater`,</if>
            `created`,
            `updated`,
            `isDeleted`
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="pojo.shipId!=null">#{pojo.shipId},</if>
            <if test="pojo.eventId!=null">#{pojo.eventId},</if>
            <if test="pojo.beginDate!=null">#{pojo.beginDate},</if>
            <if test="pojo.endDate!=null">#{pojo.endDate},</if>
            <if test="pojo.dayNum!=null">#{pojo.dayNum},</if>
            <if test="pojo.description!=null">#{pojo.description},</if>
            <if test="pojo.creator!=null">#{pojo.creator},</if>
            <if test="pojo.updater!=null">#{pojo.updater},</if>
            UNIX_TIMESTAMP(now()),
            UNIX_TIMESTAMP(now()),
            0
        </trim>
    </insert>

    <!--auto generated Code-->
    <update id="update">
        UPDATE ShipEvent
        <set>
            <if test="pojo.shipId!=null"> `shipId` = #{pojo.shipId},</if>
            <if test="pojo.eventId!=null"> `eventId`= #{pojo.eventId},</if>
            <if test="pojo.beginDate!=null"> `beginDate`= #{pojo.beginDate},</if>
            <if test="pojo.endDate!=null"> `endDate`= #{pojo.endDate},</if>
            <if test="pojo.dayNum!=null"> `dayNum`= #{pojo.dayNum},</if>
            <if test="pojo.description!=null"> `description`= #{pojo.description},</if>
            <if test="pojo.updater!=null">`updater` = #{pojo.updater},</if>
            <if test="pojo.isDeleted!=null">`isDeleted` = #{pojo.isDeleted},</if>
            `updated` = UNIX_TIMESTAMP(now())
        </set>
        WHERE `id` = #{pojo.id} and `isDeleted` = 0
        LIMIT 1
    </update>

    <update id="del" parameterType="java.lang.Long">
        UPDATE ShipEvent
        SET `isDeleted` = 1 ,
        `updated` = UNIX_TIMESTAMP(now())
        WHERE `id` = #{id} and `isDeleted` = 0
        LIMIT 1
    </update>

    <select id="getById" parameterType="java.lang.Long" resultMap="AllColumnMap">
        SELECT <include refid="all_column_user"/>
        FROM `ShipEvent` a  LEFT JOIN `UserProfile` b on a.creator = b.id
        WHERE a.`id` = #{id} and a.`isDeleted` = 0
        LIMIT 1
    </select>

    <select id="getExistCount" resultType="java.lang.Integer" parameterType="Map" >
        SELECT  count(id)
        FROM  `ShipEvent`
        where `isDeleted` = 0 AND 1 = 2
        <if test="pojo.id != null"> AND `id` != #{pojo.id} </if>
        limit 1
    </select>

    <select id="getCount" resultType="java.lang.Integer" parameterType="Map" >
        SELECT  count(a.id)
        FROM  `ShipEvent` a  LEFT JOIN `UserProfile` b on a.creator = b.id
        LEFT JOIN `Ship` c on a.shipId = c.id
        <include refid="pageQueryWhereCommon"/>
        limit 1
    </select>

    <select id="queryByPage" resultMap="AllColumnMap" parameterType="Map">
        select <include refid="all_column_user"/>
        from `ShipEvent` a LEFT JOIN `UserProfile` b on a.creator = b.id
        LEFT JOIN `Ship` c on a.shipId = c.id
        <include refid="pageQueryWhereCommon"/>
        order by a.`created` desc
        <include refid="pageCommon"/>
    </select>

    <sql id="pageQueryWhereCommon">
        where a.`isDeleted` = 0
        <if test="pojo.id != null"> AND a.`id` = #{pojo.id} </if>
        <if test="pojo.key != null">
            AND (a.`eventId` like concat('%', #{pojo.key}, '%') or c.`shipNo` like concat('%', #{pojo.key}, '%') )
        </if>
    </sql>

    <sql id="pageCommon">
        <choose>
            <when test="pojo.startRow != null and pojo.startRow gt -1 and pojo.limitNum != null and pojo.limitNum gt 0">
                limit #{pojo.startRow}, #{pojo.limitNum}
            </when>
            <otherwise>
                limit 100
            </otherwise>
        </choose>
    </sql>

</mapper>