<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.szmtjk.business.db.dao.FileImportStatusDAO">
    <resultMap id="AllColumnMap" type="com.szmtjk.business.db.entity.FileImportStatusDO">
        <result property="id" column="ID"/>
        <result property="bizType" column="BIZTYPE"/>
        <result property="fileName" column="FILENAME"/>
        <result property="fileNo" column="FILENO"/>
        <result property="status" column="STATUS"/>
        <result property="tip" column="TIP"/>
        <result property="created" column="CREATED"/>
        <result property="updated" column="UPDATED"/>
        <result property="isDeleted" column="ISDELETED"/>
    </resultMap>

    <sql id="all_column">
        `ID`
        ,`BIZTYPE`
        ,`FILENAME`
        ,`FILENO`
        ,`STATUS`
        ,`TIP`
        ,`CREATED`
        ,`UPDATED`
        ,`ISDELETED`
    </sql>

    <insert id="insertSelective" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `FileImportStatus`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            BIZTYPE,
            FILENAME,
            FILENO,
            STATUS,
            TIP,
            CREATED,
            UPDATED,
            ISDELETED,
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bizType!=null">#{bizType},</if>
            <if test="fileName!=null">#{fileName},</if>
            <if test="fileNo!=null">#{fileNo},</if>
            <if test="status!=null">#{status},</if>
            <if test="tip!=null">#{tip},</if>
            UNIX_TIMESTAMP(now()),
            UNIX_TIMESTAMP(now()),
            <if test="isDeleted!=null">#{isDeleted},</if>
        </trim>
    </insert>

    <update id="update">
        UPDATE `FileImportStatus`
        SET
        <trim prefix="" suffix="" suffixOverrides=",">
            <if test="bizType!=null">`BIZTYPE` = #{bizType},</if>
            <if test="fileName!=null">`FILENAME` = #{fileName},</if>
            <if test="fileNo!=null">`FILENO` = #{fileNo},</if>
            <if test="status!=null">`STATUS` = #{status},</if>
            <if test="tip!=null">`TIP` = #{tip},</if>
            UNIX_TIMESTAMP(now()),
            <if test="isDeleted!=null">`ISDELETED` = #{isDeleted},</if>
        </trim>
        WHERE `id` = #{id} and `isDeleted` = 0
        LIMIT 1
    </update>

    <update id="del" parameterType="java.lang.Long">
        UPDATE `FileImportStatus`
        SET `isDeleted` = 1 ,
        `updated` = UNIX_TIMESTAMP(now())
        WHERE `id` = #{id} and `isDeleted` = 0
        LIMIT 1
    </update>

    <select id="getExistCount" resultType="java.lang.Integer" parameterType="Map" >
        SELECT  count(id)
        FROM  `FileImportStatus`
        where `isDeleted` = 0
        <if test="id != null"> AND `id` != #{id} </if>
        <!-- other query condition to be added -->
        limit 1
    </select>

    <select id="getCount" resultType="java.lang.Integer" parameterType="Map" >
        SELECT  count(id)
        FROM  `FileImportStatus`
        where `isDeleted` = 0
        <!-- other query condition to be added -->
        limit 1
    </select>

    <select id="queryByPage" resultMap="AllColumnMap" parameterType="Map">
        select <include refid="all_column"/>
        from `FileImportStatus`
        where `isDeleted` = 0
        <!-- other query condition to be added -->
        order by `id` desc
        <include refid="pageCommon"/>
    </select>

    <sql id="pageCommon">
        <choose>
        <when test="startRow != null and startRow gt -1 and limitNum != null and limitNum gt 0">
            limit #{startRow}, #{limitNum}
        </when>
        <otherwise>
            limit 200
        </otherwise>
        </choose>
    </sql>
</mapper>